interface:
  display_name: "Verify Action Receipt"
  short_description: "Verify AgentProof v2 receipts safely"
  default_prompt: "Verify this AgentProof receipt and separate signature validity from signer trust."
dependencies:
  tools:
    - type: "mcp"
      value: "opstruth"
      description: "Read-only AgentProof receipt verification"
      transport: "streamable_http"
      url: "https://opstruth-chatgpt.woeinvests.workers.dev/mcp"
