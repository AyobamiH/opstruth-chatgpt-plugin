interface:
  display_name: "Verify Action Receipt"
  short_description: "Verify AgentProof and OpsTruth receipts safely"
  default_prompt: "Verify this signed receipt and separate digest validity, signature validity and signer trust."
dependencies:
  tools:
    - type: "mcp"
      value: "opstruth"
      description: "Read-only AgentProof and OpsTruth receipt verification"
      transport: "streamable_http"
      url: "https://mcp.opstruth.io/mcp"
