interface:
  display_name: "Verify Release Readiness"
  short_description: "Check CI, deployment and release evidence"
  default_prompt: "Check whether this public repository is ready for release using current CI evidence and any supplied health endpoint."
dependencies:
  tools:
    - type: "mcp"
      value: "opstruth"
      description: "Read-only CI, release and public HTTPS deployment evidence checks"
      transport: "streamable_http"
      url: "https://opstruth-chatgpt.woeinvests.workers.dev/mcp"
