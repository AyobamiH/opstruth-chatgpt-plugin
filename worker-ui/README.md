# Evidence UI

The evidence UI is embedded as the MCP Apps resource `ui://opstruth/evidence-v1.html`. Data tools return structured content without mounting UI. `opstruth_render_evidence` renders a final model-selected report.
